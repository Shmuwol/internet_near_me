require 'spec_helper'

describe InternetNearMe do
  it 'has a version number' do
    expect(InternetNearMe::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
